# Copy this file to `.env` (or `.env.local`) and fill in your values.
# All vars are EXPO_PUBLIC_* so Expo inlines them into the app at build time.
# Restart with `npx expo start -c` after changing them.
#
# These are all client-safe (public) values — the Supabase anon key is meant to
# be embedded in the app; Row-Level Security policies protect your data.

# Hosted courts.json the app fetches at launch (raw GitHub URL, GitHub Pages, or
# any CDN). Leave unset to use the bundled data only.
EXPO_PUBLIC_COURTS_URL=https://raw.githubusercontent.com/<your-username>/hoopmap/main/data/courts.json

# Supabase — enables shared, real-time crowd check-ins. Leave both unset to fall
# back to on-device (local) check-ins. Find these in your Supabase project under
# Settings → API (or the "Connect" button).
EXPO_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<your-anon-or-publishable-key>

# Sentry crash reporting (native builds only — web uses Vercel Analytics).
# Leave unset to disable entirely (lib/crash.js no-ops). From your Sentry
# project: Settings → Client Keys (DSN). DSNs are client-safe by design.
# EXPO_PUBLIC_SENTRY_DSN=https://<key>@<org-id>.ingest.us.sentry.io/<project-id>

# Local AI assistant (the Python service in chatbot/). Unset = the floating ✨
# launcher does not render at all, which is what production ships. Point it at a
# running service to turn the feature on:
#   npm run export:chatbot && cd chatbot && .venv/bin/python -m uvicorn app:app --port 8000
# Use your Mac's LAN IP instead of localhost for a physical device.
#
# NOTE: setting this in a store build sends users' questions to a model provider,
# which the privacy policy must disclose (npm run check enforces it). Coordinates
# are sent too but only measured against and dropped, so the App Store label's
# "Location: Not Collected" still holds — see docs/privacy-nutrition-label.md.
# EXPO_PUBLIC_ASSISTANT_URL=http://localhost:8000

# Shared token for that service, matching ASSISTANT_TOKEN in chatbot/.env. Only
# needed if the service sets one (it doesn't by default). Like every
# EXPO_PUBLIC_ var this is inlined into the bundle and extractable from a shipped
# app — it deters drive-by traffic; the service's rate limits are the real guard.
# EXPO_PUBLIC_ASSISTANT_TOKEN=
